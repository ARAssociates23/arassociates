// This component is now empty as the theme toggle has been removed
// We're keeping the file to avoid breaking any imports, but it doesn't render anything

export function ThemeToggle() {
  return null;
}
