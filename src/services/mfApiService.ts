
// mfApiService.ts - Simplified service without NAV fetching

import { toast } from "sonner";

/**
 * Initialize service (placeholder)
 */
export const initMfApiService = async () => {
  console.log('MF API service initialized (NAV fetching disabled)');
  toast.success('MF API service ready', { description: 'Basic functionality available' });
};
